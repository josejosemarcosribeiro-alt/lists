extends layout

block content
    .d-flex.justify-content-center
        ul.list-group(style="width: 50%;")
            //- Posts mais novos primeiro
            each list_item in list_items.reverse()
                li.list-group-item.text-center
                    a(href="/list/item/" + list_item.id)= list_item.title
